# React Native / Expo + Metro do not fully support pnpm's default symlinked
# node_modules. Use a hoisted layout so Metro can resolve packages normally.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
